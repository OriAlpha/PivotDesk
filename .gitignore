__pycache__/
.venv/
*.pyc
.pytest_cache/
.ruff_cache/
graphify-out/

# session tooling artifacts
.zcode/
.claude/

# personal notes / local reference — not tracked
pivotdesk-guide.md

# runtime state: positions, visit counts, recent searches. Written on every
# run and personal to the machine, so it is not shared history.
pivotdesk_state.json
